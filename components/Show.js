import React, {Fragment, Component} from 'react'
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native'

class Show extends Component {
  render(){
    return(
      <Text>show page</Text>
    )
  }
}

export default Show
